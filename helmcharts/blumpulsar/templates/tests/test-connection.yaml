apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blumpulsar.fullname" . }}-test-connection"
  labels:
    {{- include "blumpulsar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blumpulsar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
